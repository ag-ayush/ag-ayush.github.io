<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ayushgoel.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ayushgoel.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-21T01:14:30+00:00</updated><id>https://ayushgoel.com/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Your AI Coding Agent Changes Are Running on Vibes</title><link href="https://ayushgoel.com/blog/2026/eval-harness/" rel="alternate" type="text/html" title="Your AI Coding Agent Changes Are Running on Vibes"/><published>2026-06-16T12:00:00+00:00</published><updated>2026-06-16T12:00:00+00:00</updated><id>https://ayushgoel.com/blog/2026/eval-harness</id><content type="html" xml:base="https://ayushgoel.com/blog/2026/eval-harness/"><![CDATA[<p>We have automated tests for application code. CI pipelines. Coverage gates. Mutation testing. Years of accumulated tooling that tells us whether our software works.</p> <p>Then we plug in an AI coding agent, update the context files, tweak the tools it can use, and ship changes on feel.</p> <p>That’s the gap eval harnesses close — where “eval harness” means the measurement system that reruns frozen tasks to check whether changes to the agent harness (context files, tools, permissions, feedback loops, workflow constraints) actually helped.</p> <hr/> <h2 id="the-real-problem-no-control-group">The Real Problem: No Control Group</h2> <p>The problem isn’t that agent output is nondeterministic — though it is. The problem is deeper: <strong>in production, you never get the same scenario twice</strong>.</p> <p>You update the context files. You apply them to the next PR. That PR is a different problem, a different codebase state, a different developer writing the ticket. You can’t know whether your changes helped, hurt, or did nothing — because you changed two variables at once: the agent harness and the problem.</p> <p>That’s not an A/B test. It’s not even an experiment.</p> <p>The eval harness solves this by manufacturing the repeatable scenario on purpose. Freeze the codebase at a known commit. Freeze the task. Freeze the reference solution. Now change only the agent harness, re-run, and compare. Everything else is held constant.</p> <p>SWE-bench — the benchmark most coding agent vendors cite for capability claims — proved this pattern at scale. Each task is drawn from a real GitHub issue and corresponding pull request, with the repository frozen before the fix landed. An agent gets the issue and the codebase at that pre-fix commit. It never sees the solution. A candidate is resolved when it makes the relevant failing tests pass without breaking the relevant passing tests. Same problem, same benchmark protocol, comparable results.</p> <p>The insight transfers directly to your own codebase. You don’t need a public benchmark. You need frozen copies of real work your team already did.</p> <hr/> <h2 id="the-harness-is-half-the-agent">The Harness Is Half the Agent</h2> <p>A useful shorthand that <a href="https://martinfowler.com/articles/harness-engineering.html">Martin Fowler points to in Harness Engineering</a>:</p> <blockquote> <p>Agent = Model + Harness</p> </blockquote> <p>The model is one component. The context files, constraints, feedback loops, and instructions are the other. That matters because it means the agent by this definition is improvable without touching the model weights.</p> <p>Teams that treat context files as documentation are missing the point. They are the other half of the agent. Measuring whether changes to them actually help isn’t optional — it’s the same discipline as measuring whether a code change works. The eval harness is what makes that discipline possible.</p> <hr/> <h2 id="anatomy-of-an-eval-case">Anatomy of an Eval Case</h2> <p>Each eval case needs four things:</p> <p><strong>Parent commit.</strong> The repository state before a PR merged. The agent starts here with no knowledge of the solution.</p> <p><strong>Prompt.</strong> What should change, written at the ticket level. Avoid file names, class names, and method names unless they were part of the original request — otherwise they leak the answer. The agent should rediscover the implementation from the codebase, the same way a developer would from a well-scoped ticket.</p> <p><strong>Golden patch.</strong> The actual merged code. The agent never sees it. Used only as the reference for human or LLM review.</p> <p><strong>Mechanical checks.</strong> Deterministic gates — compilation, formatting, architecture tests — that run against the agent’s final tree regardless of what the judge says. Code that doesn’t compile hasn’t passed.</p> <p>For broad coding tasks, the eval also benefits from an LLM judge: a model that scores the agent’s output against the golden patch on a rubric. Verdicts might be <code class="language-plaintext highlighter-rouge">agent-better</code>, <code class="language-plaintext highlighter-rouge">equivalent</code>, <code class="language-plaintext highlighter-rouge">reference-better</code>, or <code class="language-plaintext highlighter-rouge">agent-missed</code>, each with a confidence level. For narrow bugs with strong tests, deterministic checks may be enough. For maintainability, architecture, and product-fit questions, the judge captures signal the test suite usually misses.</p> <hr/> <h2 id="testing-todays-harness-against-yesterdays-problems">Testing Today’s Harness Against Yesterday’s Problems</h2> <p>Evals freeze the codebase at an older commit. But you don’t want the agent seeing the harness files from six months ago, when the PR merged — you want it seeing <em>today’s</em>. That’s the entire point: you’re testing your current harness against historical problems.</p> <p>The mechanism is an overlay. A repo profile captures the build-system specifics once and copies your current harness files forward from HEAD onto the frozen checkout, then reuses that setup across every eval case for the repo:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">my-service</span>
<span class="na">default_overlay_branch</span><span class="pi">:</span> <span class="s">main</span>
<span class="na">default_overlay_from_head</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">CLAUDE.md</span> <span class="c1"># whatever your stack calls its context file</span>
  <span class="pi">-</span> <span class="s">doc/adr/</span>
<span class="na">mechanical_checks</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">compile</span>
    <span class="na">command</span><span class="pi">:</span> <span class="s">mvn compile -s settings.xml -pl :my-service-app -q -DskipTests</span>
    <span class="na">timeout_seconds</span><span class="pi">:</span> <span class="m">900</span>
</code></pre></div></div> <p>The filenames are one ecosystem’s. In Claude Code the context file is <code class="language-plaintext highlighter-rouge">CLAUDE.md</code> or files under <code class="language-plaintext highlighter-rouge">.claude/</code>; elsewhere it’s <code class="language-plaintext highlighter-rouge">AGENTS.md</code>, a system prompt, or <code class="language-plaintext highlighter-rouge">.cursorrules</code>. The mechanics don’t change — only the filenames.</p> <p><code class="language-plaintext highlighter-rouge">default_overlay_from_head</code> is the field that makes a before/after comparison mean anything. Without it, you’d be testing stale harness files against the frozen problem — measuring history, not the changes you actually want to evaluate.</p> <p>You should hold the rest of the experiment fixed too: model version, run budget, tool permissions, and judging rubric. Otherwise the harness change gets mixed with model drift, tool drift, and evaluation drift.</p> <hr/> <h2 id="reading-the-results">Reading the Results</h2> <p>The first run rarely produces a perfect score, and that’s fine. What it produces is a baseline.</p> <p>A typical first result: mechanical checks pass clean — the agent compiled, formatted correctly, cleared the deterministic gates. The LLM judge calls it <strong>reference-better, medium confidence</strong>. The human PR was tighter — fewer abstractions, more targeted changes.</p> <p>That verdict isn’t a failure. It’s information. The agent knows the codebase well enough to produce correct, compiling output; it doesn’t yet make the same architectural tradeoffs the developer made. You now know where the gap is. Update the context files, re-run this same frozen case, and watch whether the verdict moves.</p> <p>But before you trust that movement, establish a noise floor. The agent’s output is stochastic — run the same case twice with identical inputs and the score will move on its own. So run a few cases with no changes first and measure how much they drift. Any harness-driven delta smaller than that drift isn’t signal; it’s variance. A consistent shift across multiple cases, in the same direction, larger than the noise floor — that’s signal worth acting on.</p> <p>This is what you don’t get without the harness: a fixed reference to move against. Apply new context files to the next real ticket instead, and you’re comparing against a different problem and a different solution, with no way to tell the harness change from the luck of the draw.</p> <hr/> <h2 id="the-prompt-is-the-experiment">The Prompt Is the Experiment</h2> <p>The quality of an eval case is bounded by the quality of its prompt.</p> <p>Augment Code’s <a href="https://www.augmentcode.com/blog/how-to-write-good-agents-dot-md-files">research on agent context files</a> reported swings large enough to rival model choice: the best docs improved output substantially, while the worst docs made it worse than having no <code class="language-plaintext highlighter-rouge">AGENTS.md</code> at all. The lesson transfers to eval prompts. A prompt that says “fix the widget initialization” and one that explains the protocol change, the backward-compatibility constraint, and what done looks like are measuring different things. Only the second is a real experiment.</p> <p>The time spent writing a clear, ticket-level, hint-free prompt is the investment that determines what the eval can actually tell you.</p> <p>The same principle applies to the context files you’re trying to improve. Prohibitions without alternatives (“don’t instantiate HTTP clients directly”) cause agents to over-explore. Pairing them with a prescription (“use the shared <code class="language-plaintext highlighter-rouge">apiClient</code>”) closes the loop. You’ll only know if that change helped if you have an eval to run it against.</p> <hr/> <h2 id="the-judge-problem">The Judge Problem</h2> <p>Using an LLM to grade an LLM is the obvious objection, and it’s fair. The judge should not be your only gate.</p> <p>The bias is real. AI21 Labs documented a version of it while studying SWE-bench-style patch selection: an LLM judge developed a preference for “gold-like” patches — minimal, clean — even when a messier solution was functionally correct. Grading form, not function. A rubric that explicitly prioritized correctness and completeness over minimality closed most of that gap.</p> <p>But the deeper problem: how do you know the judge is calibrated? You’re stacking a stochastic evaluator on top of a stochastic agent. Left unchecked, you’re adding noise to noise.</p> <p>The practical answer is calibration. Keep a small set of obvious cases — clear pass, clear fail, equivalent-but-different, correct-but-messy, clean-but-incomplete. Verify the judge handles them before trusting it on ambiguous work. Do this periodically, and whenever the rubric changes. The rubric should be versioned; recalibrate when it changes.</p> <p>This doesn’t need to be statistically perfect on day one. It needs to be intentional. Treat the rubric the same way you’d treat a code review checklist — a living document that improves over time.</p> <hr/> <h2 id="start-small">Start Small</h2> <p>The first version does not need a platform team. Start with five merged PRs from the last quarter:</p> <ul> <li>One routine bug fix</li> <li>One feature with wiring across multiple files</li> <li>One refactor or migration</li> <li>One production issue where tests mattered</li> <li>One case where architecture judgment mattered</li> </ul> <p>For each case, capture the parent commit, the ticket-level prompt, the golden patch, and the mechanical checks. Run the current harness a few times to establish the noise floor. Then change one harness artifact — an <code class="language-plaintext highlighter-rouge">AGENTS.md</code>, a workflow instruction, a linter, a tool permission — and rerun the same cases.</p> <p>The first win is not “agents replace developers.” It’s knowing where to invest the next hour of harness work.</p> <hr/> <h2 id="this-compounds">This Compounds</h2> <p>The value of each eval case added is not linear. The first gives you a baseline. The second lets you compare across problem types. The fifth makes individual run variance matter less. The tenth gives you enough signal to detect regressions from model version changes alone.</p> <p>Teams that invest in eval infrastructure compound. Every case added makes the next harness change safer to ship. Those that treat it as an afterthought make context changes by feel indefinitely.</p> <p>The discipline is the same one that made unit testing standard practice — not the first test, but the hundredth.</p> <hr/> <h2 id="what-this-isnt">What This Isn’t</h2> <p>Eval harnesses measure whether an agent can reproduce a known-good solution given a clear prompt. They don’t:</p> <ul> <li>Test on genuinely novel problems</li> <li>Replace human code review</li> <li>Guarantee production safety</li> <li>Cover adversarial inputs</li> </ul> <p>They’re a measurement tool. The signal is directional. Acting on it still requires judgment.</p> <p>But the alternative — shipping harness changes with no signal at all — isn’t disciplined. It’s guessing.</p> <hr/> <p><em>Further reading:</em></p> <ul> <li><a href="https://martinfowler.com/articles/harness-engineering.html">Harness Engineering — Martin Fowler</a></li> <li><a href="https://openai.com/index/harness-engineering/">Harness Engineering — OpenAI</a></li> <li><a href="https://www.augmentcode.com/blog/how-to-write-good-agents-dot-md-files">How to Write Good AGENTS.md Files — Augment Code</a></li> <li><a href="https://www.ai21.com/blog/gold-like-answers-benchmarks/">Gold-Like Answers Reveal LLM Judge Bias — AI21 Labs</a></li> <li><a href="https://www.swebench.com/">SWE-bench</a></li> </ul>]]></content><author><name></name></author><category term="engineering"/><category term="ai"/><category term="agents"/><category term="evals"/><category term="engineering"/><summary type="html"><![CDATA[AI coding agents are only half model. The other half is the harness around them, and evals are how teams stop changing that harness by feel.]]></summary></entry><entry><title type="html">Scaling Our Microservice Made Latency Worse - Here’s How We Fixed It</title><link href="https://ayushgoel.com/blog/2026/prescaling-latency-backfire/" rel="alternate" type="text/html" title="Scaling Our Microservice Made Latency Worse - Here’s How We Fixed It"/><published>2026-02-15T12:00:00+00:00</published><updated>2026-02-15T12:00:00+00:00</updated><id>https://ayushgoel.com/blog/2026/prescaling-latency-backfire</id><content type="html" xml:base="https://ayushgoel.com/blog/2026/prescaling-latency-backfire/"><![CDATA[<p>We scale for reliability, but low traffic can leave connections idle, and idle connections will eventually be terminated. When that happens, the extra capacity can backfire.</p> <h2 id="the-problem">The Problem</h2> <p>We have a service that’s high-throughput, up to ~50k TPS, and latency-sensitive. It sits at the bottom of the stack - a foundational dependency that many upstream services call, so its latency compounds. 99% of traffic to this service is read traffic, and the remainder involves writes. The write path is where the problem showed up.</p> <p>We prescale so we’re not iced out of EC2 capacity and so we have capacity ready for regional failover during business-critical times. The tradeoff is that we often have more pods than current traffic needs. Many pods see low or sporadic traffic, and on the write path that means long gaps between requests.</p> <p>The chart below shows write TPS on a single pod over a 5-minute window when the service is scaled up. Gaps of 30 seconds or more between writes are evident.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/write_tps_single_pod_5m-480.webp 480w,/assets/img/write_tps_single_pod_5m-800.webp 800w,/assets/img/write_tps_single_pod_5m-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/write_tps_single_pod_5m.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Write TPS on a single pod over a 5-minute window when scaled up. Gaps of 30+ seconds between writes are evident.</figcaption> </figure> <p>We observed elevated p99 latency in production from that prescaled capacity. We traced the increase in latency to DynamoDB writes and from there to the connection lifecycle of the DynamoDB client. Here’s the chain we saw:</p> <ol> <li><strong>Idle connections closed.</strong> On pods with little traffic, idle DynamoDB connections were closed.</li> <li><strong>Short client timeout.</strong> We had chosen UrlConnectionHttpClient for its lightweight footprint and quick start time, which let pods come up slightly faster than with ApacheHttpClient. That client closes idle connections after ~5 seconds and doesn’t let you configure it (<a href="https://github.com/aws/aws-sdk-java-v2/issues/3941">GitHub #3941</a>).</li> <li><strong>New connections on demand.</strong> When a request finally arrived, it often had to establish a new connection to DynamoDB (TCP + TLS + metadata). Creating connections on demand is expensive: cold connections cost on the order of 40–80 ms; reused connections are ~1–3 ms. At scale, that gap is massive.</li> <li><strong>Latency spike.</strong> That setup cost showed up in p99.</li> </ol> <p>What we missed: we’d prescaled for reliability but hadn’t kept connections warm. The HTTP client we’d chosen for fast startup had a short idle timeout, which made the prescaling backfire.</p> <h2 id="the-solution">The Solution</h2> <p>The fix was an HTTP client with configurable idle timeout and TCP keepalive.</p> <p>We made three changes:</p> <ol> <li><strong>Switch to ApacheHttpClient</strong> for all DynamoDB clients (we switched everywhere to maintain consistency).</li> <li><strong>Enable TCP keepalive</strong> so idle connections are less likely to be torn down by the network or the server.</li> <li><strong>Set a configurable connection idle timeout</strong> (e.g. 60 seconds) so we control when the client closes idle connections instead of being stuck at ~5s.</li> </ol> <p>No synthetic warmup traffic, no change to scaling policy - just picking the right HTTP client and tuning connection lifecycle.</p> <h2 id="implementation-details">Implementation Details</h2> <h3 id="why-the-write-path">Why the write path?</h3> <p>The service uses separate DynamoDB clients for reads and writes, with different timeout and retry SLAs. The <strong>read client</strong> is tuned for low latency (aggressive retries) and is used in health checks that run at least every 30 seconds, so its connections stay warm. The <strong>write client</strong> is tuned for reliability (longer timeouts so writes can complete), is used less often, and has no regular traffic like the health check. Its connections idle out. When a write arrives, it often pays new-connection cost. So the increase in p99 was coming from the write path.</p> <h3 id="why-we-were-on-urlconnectionhttpclient-in-the-first-place">Why we were on UrlConnectionHttpClient in the first place</h3> <p>Engineers had chosen UrlConnectionHttpClient for its lightweight footprint and quick start time, so pods could come up faster than with ApacheHttpClient. This helped with cold start but was the wrong tradeoff for this failure mode. UrlConnectionHttpClient has a ~5 second idle connection timeout that isn’t configurable; under prescale and low traffic, connections closed quickly and new requests paid full setup cost. The right fix was connection lifecycle (keepalive + configurable idle timeout), not a lighter or faster-starting client.</p> <h3 id="options-we-didnt-take">Options we didn’t take</h3> <p><strong>Adding writes to the periodic app health checks</strong>, like the reads already have, would keep the write client’s connections warm - but we didn’t do it. The cost compounds: one write every 30 seconds × 200 pods per region × 2 regions would add a lot of extra throughput and table load for the benefit.</p> <p><strong>Scheduled scaling.</strong> Scale up only when we expect traffic to be high. The organization is working on this. It wasn’t available to us for this fix, so we didn’t depend on it.</p> <h2 id="results">Results</h2> <p>Overall P99 improved from 220ms to 100ms. We stopped observing latency increases when scaling up. Some upstream clients noticed an improvement of 200ms due to compounded benefit from multiple calls to our service.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/upstream_latency_after_fixes-480.webp 480w,/assets/img/upstream_latency_after_fixes-800.webp 800w,/assets/img/upstream_latency_after_fixes-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/upstream_latency_after_fixes.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">One upstream client's p95 and p99 latency improvement after the fix.</figcaption> </figure> <h2 id="conclusion">Conclusion</h2> <p>We added capacity for business-critical traffic peaks, but prescaling without keeping connections warm can backfire. When traffic stayed low, connections sat idle and the HTTP client we’d chosen for fast pod startup closed them in ~5 seconds, so new requests paid the connection-setup cost and p99 went up. The fix was switching to an HTTP client with configurable idle timeout and TCP keepalive, not synthetic traffic or a different scaling strategy.</p> <p>For services like ours - high-throughput and latency-sensitive - connection lifecycle matters as much as capacity. The same choice that helped with cold start (a lighter client) was the wrong one once idle connections started getting terminated.</p>]]></content><author><name></name></author><category term="engineering"/><category term="dynamodb"/><category term="performance"/><category term="aws"/><category term="architecture"/><summary type="html"><![CDATA[We prescale for EC2 headroom and regional failover during business-critical times. Low traffic left DynamoDB connections idle and the HTTP client we'd chosen for fast pod startup closed them in ~5s. Here's how we fixed p99 with configurable idle timeout and TCP keepalive.]]></summary></entry><entry><title type="html">Stop Writing Data You Never Read</title><link href="https://ayushgoel.com/blog/2026/authentication-attempt-caching/" rel="alternate" type="text/html" title="Stop Writing Data You Never Read"/><published>2026-01-04T12:00:00+00:00</published><updated>2026-01-04T12:00:00+00:00</updated><id>https://ayushgoel.com/blog/2026/authentication-attempt-caching</id><content type="html" xml:base="https://ayushgoel.com/blog/2026/authentication-attempt-caching/"><![CDATA[<h2 id="the-problem">The Problem</h2> <p>We had a seemingly simple requirement: track login attempts for account lockout policies. Every successful login generated three database writes (primary table + two GSI updates). At peak traffic, we hit 2,600 TPS with 85 million operations daily, not counting TTL deletes. This created four problems:</p> <ol> <li><strong>Write Amplification</strong>: Each success attempt generated 3 writes (primary table + 2 GSI updates), replicated across regions</li> <li><strong>Cost Pressure</strong>: Despite being relatively small in data size, TTL deletions, cross-region replication, and multiple GSIs made this table disproportionately expensive</li> <li><strong>Availability Risk</strong>: Intermittent AWS service degradation caused error spikes that were compounded by upstream retries, affecting even successful logins</li> <li><strong>GSI Partition Risk</strong>: The GSI stores a secondary identifier shared across all entries for a user. With DynamoDB’s 10 GB limit per partition and increasing write traffic, we risked hitting capacity constraints that would throttle writes for affected users</li> </ol> <p>The aha moment: we had a write-heavy operation for temporal data we rarely read. We only needed this data when logins failed, but with a high success rate, we were slowing down the happy path for everyone with no real benefit.</p> <h2 id="the-solution">The Solution</h2> <p>We realized that <strong>successful attempts only indicate the absence of consecutive failures</strong>. We didn’t need a complete history, just recent state.</p> <p>This insight enabled a simple caching strategy:</p> <ol> <li>Cache successful attempts in Redis with a 24-hour TTL (±10% jitter)</li> <li>On failed attempts, clear the Redis cache and trigger lockout policy calculations using DynamoDB</li> <li>Keep DynamoDB as the source of truth with Redis as a soft dependency</li> </ol> <h3 id="architecture-before">Architecture: Before</h3> <pre><code class="language-mermaid">sequenceDiagram
    participant Client
    participant Service
    participant DynamoDB

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;DynamoDB: Write Success Attempt
    Note over DynamoDB: 1 write + 2 GSI writes&lt;br/&gt;+ TTL delete after 48h

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;DynamoDB: Write Success Attempt

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;DynamoDB: Write Success Attempt

    Note over Service,DynamoDB: Heavy users: 1000s of writes/day
</code></pre> <h3 id="architecture-after">Architecture: After</h3> <pre><code class="language-mermaid">sequenceDiagram
    participant Client
    participant Service
    participant Redis
    participant DynamoDB

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;Redis: Check last attempt
    Redis--&gt;&gt;Service: Not found
    Service-&gt;&gt;DynamoDB: Write Success Attempt
    Service-&gt;&gt;Redis: Cache Success (24h TTL)

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;Redis: Check last attempt
    Redis--&gt;&gt;Service: Success (cache hit)
    Note over Service: Skip write - already successful

    Client-&gt;&gt;Service: Log In (Failure)
    Service-&gt;&gt;DynamoDB: Write Failure Attempt
    Service-&gt;&gt;DynamoDB: Calculate lockout policy
    Note over DynamoDB: Read recent attempts&lt;br/&gt;Apply lockout rules
    Service-&gt;&gt;Redis: Delete cached success
</code></pre> <h2 id="implementation-details">Implementation Details</h2> <h3 id="what-to-cache">What to cache?</h3> <p>The question: cache everything, cache failures only, or cache successes only?</p> <p><strong>Option 1: Migrate entirely to Redis and eliminate DynamoDB (✗)</strong></p> <p>Complete cache coverage, but this requires managing multiple failure events per user with individual TTLs. We evaluated Redis HASHes with per-field expiration (<code class="language-plaintext highlighter-rouge">HEXPIRE</code>) to store all attempts in one structure:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>digid:attempts → {attemptId1: failureData, attemptId2: failureData, success: successData}
</code></pre></div></div> <p><strong>Blocker:</strong> Our deployed version of Redis doesn’t support <code class="language-plaintext highlighter-rouge">HEXPIRE</code> (<a href="https://github.com/valkey-io/valkey/issues/2778">Issue #2778</a>). Without this, we’d need separate Redis keys per failure attempt (memory inefficient) with cleanup jobs (operational complexity) to prevent stale objects.</p> <p>We’d also need a dedicated Redis cluster with <code class="language-plaintext highlighter-rouge">volatile-ttl</code> eviction policy since our existing cluster uses <code class="language-plaintext highlighter-rouge">allkeys-lru</code>. Standing up new infrastructure wasn’t justified.</p> <p><strong>Option 2: Cache only failures (✗)</strong></p> <p>Fewer events to cache since failures are less frequent.</p> <p><strong>Rejected.</strong> High-volume users like automated systems generate most of the write pressure through repeated successful logins. Caching failures doesn’t solve the problem.</p> <p><strong>Option 3: Cache only successes (✓)</strong></p> <p>Single Redis key per user with simple SET/GET/DELETE operations. This addresses the dominant traffic pattern (repeated successes). Lockout calculations still use DynamoDB on failures, which is fine since failures are infrequent.</p> <h3 id="soft-dependency-pattern">Soft Dependency Pattern</h3> <p>We designed the caching layer to fail gracefully. If Redis has issues, logins still work. The implementation:</p> <ul> <li>40ms read/write timeouts for fast fallback</li> <li>Treat any Redis error as a cache miss (triggers DynamoDB write)</li> <li>Circuit breakers for automatic failover during degradation</li> <li>Feature flag as a kill switch</li> </ul> <p>This gives us optimistic caching with pessimistic error handling.</p> <h3 id="ttl-strategy">TTL Strategy</h3> <p>We chose a 24-hour TTL with ±10% jitter to prevent cache expiration thundering herds.</p> <h2 id="results">Results</h2> <table> <thead> <tr> <th>Metric</th> <th>Improvement</th> </tr> </thead> <tbody> <tr> <td><strong>PreProd Write Volume</strong></td> <td>98% reduction</td> </tr> <tr> <td><strong>Production Write Volume</strong></td> <td>75% reduction</td> </tr> <tr> <td><strong>DynamoDB Consumed Write Units</strong></td> <td>120k → 20k baseline<br/>320k → 60k peak</td> </tr> <tr> <td><strong>Cost Impact</strong></td> <td>~90% reduction in DynamoDB costs</td> </tr> </tbody> </table> <p>The graph below shows the production impact over time. The sharp drop in July 2025 marks when we rolled out the caching solution:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/authentication_attempt_result-480.webp 480w,/assets/img/authentication_attempt_result-800.webp 800w,/assets/img/authentication_attempt_result-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/authentication_attempt_result.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Write Capacity Units consumed by the DynamoDB table in production over one year, showing the impact of caching successful attempts.</figcaption> </figure> <p>The difference in improvement between environments reflects traffic patterns. Test account reuse in preproduction creates higher cache hit rates, while production sees more unique daily logins.</p> <h2 id="conclusion">Conclusion</h2> <p>Lockout policies only need recent attempt state, not complete history. Recognizing this let us reduce database writes by 75-98% while improving availability. We were storing every successful login when we actually just needed proof that consecutive failures hadn’t occurred.</p> <p>Good architecture comes from understanding your access patterns and working within platform constraints. Write amplification compounds fast with TTL deletes, GSI replication, and high throughput, even for small tables. The soft dependency pattern kept our critical authentication path reliable while Redis gave us the performance wins. And sometimes the biggest gains come from questioning what you’re storing in the first place.</p>]]></content><author><name></name></author><category term="engineering"/><category term="dynamodb"/><category term="performance"/><category term="aws"/><category term="architecture"/><category term="caching"/><category term="redis"/><summary type="html"><![CDATA[At 2,600 TPS, we were executing 85M writes daily for data we rarely read. Here's how understanding our access pattern reduced writes by 75% using caching.]]></summary></entry></feed>