<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ayushgoel.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ayushgoel.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-02-17T02:57:07+00:00</updated><id>https://ayushgoel.com/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Scaling Our Microservice Made Latency Worse - Here’s How We Fixed It</title><link href="https://ayushgoel.com/blog/2026/prescaling-latency-backfire/" rel="alternate" type="text/html" title="Scaling Our Microservice Made Latency Worse - Here’s How We Fixed It"/><published>2026-02-15T12:00:00+00:00</published><updated>2026-02-15T12:00:00+00:00</updated><id>https://ayushgoel.com/blog/2026/prescaling-latency-backfire</id><content type="html" xml:base="https://ayushgoel.com/blog/2026/prescaling-latency-backfire/"><![CDATA[<p>We scale for reliability, but low traffic can leave connections idle—and idle connections will eventually be terminated. When that happens, the extra capacity can backfire.</p> <h2 id="the-problem">The Problem</h2> <p>We have a service that’s high-throughput, up to ~50k TPS, and latency-sensitive. It sits at the bottom of the stack—a foundational dependency that many upstream services call, so its latency compounds. 99% of traffic to this service is read traffic, and the remainder involves writes. The write path is where the problem showed up.</p> <p>We prescale for seasonal peaks so we’re not iced out of EC2 capacity and so we have capacity ready for regional failover during business-critical times. The tradeoff is that we often have more pods than current traffic needs. Many pods see low or sporadic traffic, and on the write path that means long gaps between requests.</p> <p>The chart below shows write TPS on a single pod over a 5-minute window when the service is scaled up. Gaps of 30 seconds or more between writes are evident.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/write_tps_single_pod_5m-480.webp 480w,/assets/img/write_tps_single_pod_5m-800.webp 800w,/assets/img/write_tps_single_pod_5m-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/write_tps_single_pod_5m.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Write TPS on a single pod over a 5-minute window when scaled up. Gaps of 30+ seconds between writes are evident.</figcaption> </figure> <p>We observed elevated p99 latency in production from that prescaled capacity. We traced the increase in latency to DynamoDB writes and from there to the connection lifecycle of the DynamoDB client. Here’s the chain we saw:</p> <ol> <li><strong>Idle connections closed.</strong> On pods with little traffic, idle DynamoDB connections were closed.</li> <li><strong>Short client timeout.</strong> We had chosen UrlConnectionHttpClient for its light weight and quick start time, which let pods come up slightly faster than with ApacheHttpClient. That client closes idle connections after ~5 seconds and doesn’t let you configure it (<a href="https://github.com/aws/aws-sdk-java-v2/issues/3941">GitHub #3941</a>).</li> <li><strong>New connections on demand.</strong> When a request finally arrived, it often had to establish a new connection to DynamoDB. Creating new TCP connections is expensive—cold connections cost on the order of 40–80 ms; reused connections are ~1–3 ms. At scale, that gap is massive.</li> <li><strong>Latency spike.</strong> That setup cost showed up in p99.</li> </ol> <p>What we missed: we’d prescaled for reliability but hadn’t kept connections warm. The HTTP client we’d chosen for fast startup had a short idle timeout, which made the prescaling backfire.</p> <h2 id="the-solution">The Solution</h2> <p>The fix was an HTTP client with configurable idle timeout and TCP keepalive.</p> <p>We made three changes:</p> <ol> <li><strong>Switch to ApacheHttpClient</strong> for all DynamoDB clients (we switched everywhere to maintain consistency).</li> <li><strong>Enable TCP keepalive</strong> so idle connections are less likely to be torn down by the network or the server.</li> <li><strong>Set a configurable connection idle timeout</strong> (e.g. 60 seconds) so we control when the client closes idle connections instead of being stuck at ~5s.</li> </ol> <p>No synthetic warmup traffic, no change to scaling policy—just picking the right HTTP client and tuning connection lifecycle.</p> <h2 id="implementation-details">Implementation Details</h2> <h3 id="why-the-write-path">Why the write path?</h3> <p>The service uses separate DynamoDB clients for reads and writes, with different timeout and retry SLAs. The <strong>read client</strong> is tuned for low latency (aggressive retries) and is used in health checks that run at least every 30 seconds, so its connections stay warm. The <strong>write client</strong> is tuned for reliability (longer timeouts so writes can complete), is used less often, and has no regular traffic like the health check. Its connections idle out. When a write arrives, it often pays new-connection cost. So the increase in p99 was coming from the write path.</p> <h3 id="why-we-were-on-urlconnectionhttpclient-in-the-first-place">Why we were on UrlConnectionHttpClient in the first place</h3> <p>Engineers had chosen UrlConnectionHttpClient for its lightweight footprint and quick start time, so pods could come up faster than with ApacheHttpClient. This helped with cold start but was the wrong tradeoff for this failure mode. UrlConnectionHttpClient has a ~5 second idle connection timeout that isn’t configurable; under prescale and low traffic, connections closed quickly and new requests paid full setup cost. The right fix was connection lifecycle (keepalive + configurable idle timeout), not a lighter or faster-starting client.</p> <h3 id="options-we-didnt-take">Options we didn’t take</h3> <p><strong>Adding writes to the periodic app health checks</strong>, like the reads already have, would keep the write client’s connections warm. However, the cost compounds: one write every 30 seconds × 200 pods per region × 2 regions would add a lot of extra throughput and table load for the benefit.</p> <p><strong>Scheduled scaling.</strong> Scale up only when we expect traffic to be high. The organization is working on this. It wasn’t available to us for this fix, so we didn’t depend on it.</p> <h2 id="results">Results</h2> <p>Overall P99 improved from 220ms to 100ms. We stopped observing latency increases when scaling up. Some upstream clients noticed an improvement of 200ms due to compounded benefit from multiple calls to our service.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/upstream_latency_after_fixes-480.webp 480w,/assets/img/upstream_latency_after_fixes-800.webp 800w,/assets/img/upstream_latency_after_fixes-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/upstream_latency_after_fixes.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">One upstream client's p95 and p99 latency improvement after the fix.</figcaption> </figure> <h2 id="conclusion">Conclusion</h2> <p>We added capacity for business-critical traffic peaks, but prescaling without keeping connections warm can backfire. When traffic stayed low, connections sat idle and the HTTP client we’d chosen for fast pod startup closed them in ~5 seconds, so new requests paid the connection-setup cost and p99 went up. The fix was switching to an HTTP client with configurable idle timeout and TCP keepalive, not synthetic traffic or a different scaling strategy.</p> <p>For services like ours—high-throughput and latency-sensitive—connection lifecycle matters as much as capacity. The same choice that helped with cold start (a lighter client) was the wrong one once idle connections started getting terminated.</p>]]></content><author><name></name></author><category term="engineering"/><category term="dynamodb"/><category term="performance"/><category term="aws"/><category term="architecture"/><summary type="html"><![CDATA[We prescale for EC2 headroom and regional failover during business-critical times. Low traffic left DynamoDB connections idle and the HTTP client we'd chosen for fast pod startup closed them in ~5s. Here's how we fixed p99 with configurable idle timeout and TCP keepalive.]]></summary></entry><entry><title type="html">Stop Writing Data You Never Read</title><link href="https://ayushgoel.com/blog/2026/authentication-attempt-caching/" rel="alternate" type="text/html" title="Stop Writing Data You Never Read"/><published>2026-01-04T12:00:00+00:00</published><updated>2026-01-04T12:00:00+00:00</updated><id>https://ayushgoel.com/blog/2026/authentication-attempt-caching</id><content type="html" xml:base="https://ayushgoel.com/blog/2026/authentication-attempt-caching/"><![CDATA[<h2 id="the-problem">The Problem</h2> <p>We had a seemingly simple requirement: track login attempts for account lockout policies. Every successful login generated three database writes (primary table + two GSI updates). At peak traffic, we hit 2,600 TPS with 85 million operations daily, not counting TTL deletes. This created four problems:</p> <ol> <li><strong>Write Amplification</strong>: Each success attempt generated 3 writes (primary table + 2 GSI updates), replicated across regions</li> <li><strong>Cost Pressure</strong>: Despite being relatively small in data size, TTL deletions, cross-region replication, and multiple GSIs made this table disproportionately expensive</li> <li><strong>Availability Risk</strong>: Intermittent AWS service degradation caused error spikes that were compounded by upstream retries, affecting even successful logins</li> <li><strong>GSI Partition Risk</strong>: The GSI stores a secondary identifier shared across all entries for a user. With DynamoDB’s 10 GB limit per partition and increasing write traffic, we risked hitting capacity constraints that would throttle writes for affected users</li> </ol> <p>The aha moment: we had a write-heavy operation for temporal data we rarely read. We only needed this data when logins failed, but with a high success rate, we were slowing down the happy path for everyone with no real benefit.</p> <h2 id="the-solution">The Solution</h2> <p>We realized that <strong>successful attempts only indicate the absence of consecutive failures</strong>. We didn’t need a complete history, just recent state.</p> <p>This insight enabled a simple caching strategy:</p> <ol> <li>Cache successful attempts in Redis with a 24-hour TTL (±10% jitter)</li> <li>On failed attempts, clear the Redis cache and trigger lockout policy calculations using DynamoDB</li> <li>Keep DynamoDB as the source of truth with Redis as a soft dependency</li> </ol> <h3 id="architecture-before">Architecture: Before</h3> <pre><code class="language-mermaid">sequenceDiagram
    participant Client
    participant Service
    participant DynamoDB

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;DynamoDB: Write Success Attempt
    Note over DynamoDB: 1 write + 2 GSI writes&lt;br/&gt;+ TTL delete after 48h

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;DynamoDB: Write Success Attempt

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;DynamoDB: Write Success Attempt

    Note over Service,DynamoDB: Heavy users: 1000s of writes/day
</code></pre> <h3 id="architecture-after">Architecture: After</h3> <pre><code class="language-mermaid">sequenceDiagram
    participant Client
    participant Service
    participant Redis
    participant DynamoDB

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;Redis: Check last attempt
    Redis--&gt;&gt;Service: Not found
    Service-&gt;&gt;DynamoDB: Write Success Attempt
    Service-&gt;&gt;Redis: Cache Success (24h TTL)

    Client-&gt;&gt;Service: Log In (Success)
    Service-&gt;&gt;Redis: Check last attempt
    Redis--&gt;&gt;Service: Success (cache hit)
    Note over Service: Skip write - already successful

    Client-&gt;&gt;Service: Log In (Failure)
    Service-&gt;&gt;DynamoDB: Write Failure Attempt
    Service-&gt;&gt;DynamoDB: Calculate lockout policy
    Note over DynamoDB: Read recent attempts&lt;br/&gt;Apply lockout rules
    Service-&gt;&gt;Redis: Delete cached success
</code></pre> <h2 id="implementation-details">Implementation Details</h2> <h3 id="what-to-cache">What to cache?</h3> <p>The question: cache everything, cache failures only, or cache successes only?</p> <p><strong>Option 1: Migrate entirely to Redis and eliminate DynamoDB (✗)</strong></p> <p>Complete cache coverage, but this requires managing multiple failure events per user with individual TTLs. We evaluated Redis HASHes with per-field expiration (<code class="language-plaintext highlighter-rouge">HEXPIRE</code>) to store all attempts in one structure:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>digid:attempts → {attemptId1: failureData, attemptId2: failureData, success: successData}
</code></pre></div></div> <p><strong>Blocker:</strong> Our deployed version of Redis doesn’t support <code class="language-plaintext highlighter-rouge">HEXPIRE</code> (<a href="https://github.com/valkey-io/valkey/issues/2778">Issue #2778</a>). Without this, we’d need separate Redis keys per failure attempt (memory inefficient) with cleanup jobs (operational complexity) to prevent stale objects.</p> <p>We’d also need a dedicated Redis cluster with <code class="language-plaintext highlighter-rouge">volatile-ttl</code> eviction policy since our existing cluster uses <code class="language-plaintext highlighter-rouge">allkeys-lru</code>. Standing up new infrastructure wasn’t justified.</p> <p><strong>Option 2: Cache only failures (✗)</strong></p> <p>Fewer events to cache since failures are less frequent.</p> <p><strong>Rejected.</strong> High-volume users like automated systems generate most of the write pressure through repeated successful logins. Caching failures doesn’t solve the problem.</p> <p><strong>Option 3: Cache only successes (✓)</strong></p> <p>Single Redis key per user with simple SET/GET/DELETE operations. This addresses the dominant traffic pattern (repeated successes). Lockout calculations still use DynamoDB on failures, which is fine since failures are infrequent.</p> <h3 id="soft-dependency-pattern">Soft Dependency Pattern</h3> <p>We designed the caching layer to fail gracefully. If Redis has issues, logins still work. The implementation:</p> <ul> <li>40ms read/write timeouts for fast fallback</li> <li>Treat any Redis error as a cache miss (triggers DynamoDB write)</li> <li>Circuit breakers for automatic failover during degradation</li> <li>Feature flag as a kill switch</li> </ul> <p>This gives us optimistic caching with pessimistic error handling.</p> <h3 id="ttl-strategy">TTL Strategy</h3> <p>We chose a 24-hour TTL with ±10% jitter to prevent cache expiration thundering herds.</p> <h2 id="results">Results</h2> <table> <thead> <tr> <th>Metric</th> <th>Improvement</th> </tr> </thead> <tbody> <tr> <td><strong>PreProd Write Volume</strong></td> <td>98% reduction</td> </tr> <tr> <td><strong>Production Write Volume</strong></td> <td>75% reduction</td> </tr> <tr> <td><strong>DynamoDB Consumed Write Units</strong></td> <td>120k → 20k baseline<br/>320k → 60k peak</td> </tr> <tr> <td><strong>Cost Impact</strong></td> <td>~90% reduction in DynamoDB costs</td> </tr> </tbody> </table> <p>The graph below shows the production impact over time. The sharp drop in July 2025 marks when we rolled out the caching solution:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/authentication_attempt_result-480.webp 480w,/assets/img/authentication_attempt_result-800.webp 800w,/assets/img/authentication_attempt_result-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/authentication_attempt_result.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Write Capacity Units consumed by the DynamoDB table in production over one year, showing the impact of caching successful attempts.</figcaption> </figure> <p>The difference in improvement between environments reflects traffic patterns. Test account reuse in preproduction creates higher cache hit rates, while production sees more unique daily logins.</p> <h2 id="conclusion">Conclusion</h2> <p>Lockout policies only need recent attempt state, not complete history. Recognizing this let us reduce database writes by 75-98% while improving availability. We were storing every successful login when we actually just needed proof that consecutive failures hadn’t occurred.</p> <p>Good architecture comes from understanding your access patterns and working within platform constraints. Write amplification compounds fast with TTL deletes, GSI replication, and high throughput, even for small tables. The soft dependency pattern kept our critical authentication path reliable while Redis gave us the performance wins. And sometimes the biggest gains come from questioning what you’re storing in the first place.</p>]]></content><author><name></name></author><category term="engineering"/><category term="dynamodb"/><category term="performance"/><category term="aws"/><category term="architecture"/><category term="caching"/><category term="redis"/><summary type="html"><![CDATA[At 2,600 TPS, we were executing 85M writes daily for data we rarely read. Here's how understanding our access pattern reduced writes by 75% using caching.]]></summary></entry></feed>